import CogitoCore.ARC.Definitions

namespace CogitoCore.ARC.Transformations

/-!
Reusable grid transformations will live here once we extract behavior that truly spans
multiple ARC tasks. The current solutions fold task-specific logic into their own files
to avoid polluting this shared module.
-/

end CogitoCore.ARC.Transformations
